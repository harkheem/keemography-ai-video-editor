# Core
streamlit
python-dotenv
Pillow
numpy<2.0
decorator==5.1.1

# MoviePy stack (explicit)
moviepy==1.0.3
imageio==2.31.1
imageio-ffmpeg==0.4.9
proglog==0.1.10

# CV / scene detection
opencv-python-headless==4.8.1.78
scenedetect==0.6.7

# NLP / models (can be heavy on first build)
sentence-transformers==2.2.2
torch==2.2.2+cpu --extra-index-url https://download.pytorch.org/whl/cpu
openai-whisper==20231117
